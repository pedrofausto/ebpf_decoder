[workspace]
members = [
    "userspace/loader",
    "userspace/decoder",
    "userspace/config_injector",
    "userspace/common",
]
resolver = "2"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true
